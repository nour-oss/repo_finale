{% extends 'base1.html.twig' %}

{% block title %}Examen{% endblock %}

{% block body %}
    <h1>Examen</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ examan.id }}</td>
            </tr>
            <tr>
                <th>FormationId</th>
                <td>{{ examan.formationId }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ examan.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('examen_index') }}">back to list</a>

    <a href="{{ path('examen_edit', {'id': examan.id}) }}">edit</a>

    {{ include('examen/_delete_form.html.twig') }}
{% endblock %}
