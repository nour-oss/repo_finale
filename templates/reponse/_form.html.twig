{{ form_start(form) }}


<div class="card-body">
	<div class="form-group">
		<label for="exampleInputEmail1">Réponse</label>
		{{form_widget(form.reponse, {'attr' : {'class' : 'form-control' }})}}
	</div>

	<div class="form-group">
		<label for="exampleInputEmail1">Vrai ?</label>
		{{form_widget(form.vrai, {'attr' : {'class' : 'form-control' }})}}
	</div>
	<div class="form-group">
		<label for="">Question</label>
		<select class="form-control" name="formation">
            {% for formation in formations %}
			<option>{{formation.question}}</option>                
            {% endfor %}
		</select>
	</div>

</div>
<button class="btn btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
