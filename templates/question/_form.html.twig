{{ form_start(form) }}


<div class="card-body">
	<div class="form-group">
		<label for="exampleInputEmail1">Question</label>
		{{form_widget(form.question, {'attr' : {'class' : 'form-control' }})}}
	</div>
	<div class="form-group">
		<label for="exampleInputPassword1">Formation</label>
		{{form_widget(form.formationId, {'attr' : {'class' : 'form-control' }})}}
	</div>

	
</div>
<button class="btn btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

<script>
var question = document.getElementById('question_question')

if (question.value == "") {
	window.alert("La question est vide !")	;
	return
}
</script>