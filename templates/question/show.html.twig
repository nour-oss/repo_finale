{% extends 'base1.html.twig' %}

{% block title %}Question{% endblock %}

{% block body %}
    <h1>Question</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question.id }}</td>
            </tr>
            <tr>
                <th>FormationId</th>
                <td>{{ question.formationId }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ question.question }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('question_index') }}" class="btn btn-primary">Retourner à la liste</a>

    <a href="{{ path('question_edit', {'id': question.id}) }}"class="btn btn-info">Modifier</a>

    {{ include('question/_delete_form.html.twig') }}
{% endblock %}
